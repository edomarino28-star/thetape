name: Build and publish

# Rebuild once a day and publish the static result. The site never fetches on
# behalf of a visitor -- one build serves everyone, which is what keeps this
# within SEC's rate limits no matter how much traffic the page gets.
on:
  schedule:
    - cron: "30 11 * * 1-5"     # 11:30 UTC, weekdays (after overnight filings)
  workflow_dispatch:             # and a manual "Run workflow" button
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30        # fail visibly instead of hanging for hours
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install -r requirements.txt

      - name: Build the site
        env:
          # SEC asks automated clients to identify themselves. Set this as a
          # repository variable (Settings > Secrets and variables > Actions).
          SEC_CONTACT: ${{ vars.SEC_CONTACT }}
          # Pages serves a project repo at https://OWNER.github.io/REPO
          SITE_URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}
          # Kept modest so a build finishes in minutes. SEC throttles shared CI
          # IPs, and every throttled request costs retry time, so fetching less
          # is what keeps this reliable rather than what makes it fast.
          INSIDER_DAYS: "10"
          CONGRESS_DAYS: "45"
          MAX_PTRS: "25"
          MAX_FILINGS: "250"
        run: python build_site.py

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
